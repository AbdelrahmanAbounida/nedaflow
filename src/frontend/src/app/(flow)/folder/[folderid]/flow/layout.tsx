import React, { ReactNode } from "react";

// TODO >> Start here with React Flow
// Auto generate schema between frontend and backend
const FlowLayout = ({ children }: { children: ReactNode }) => {
  return (
    <div className="flex items-start bg-red-50 w-full h-screen justify-start">
      <div className="bg-white h-full w-60"> sidebar</div>
      <div className="">paeg</div>
    </div>
  );
};

export default FlowLayout;
